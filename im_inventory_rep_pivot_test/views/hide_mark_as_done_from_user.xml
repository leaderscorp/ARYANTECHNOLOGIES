<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Inherit Form View to Modify it -->
        <record id="hide_button_from_user" model="ir.ui.view">
            <field name="name">hide_button_from_user</field>
            <field name="model">stock.picking</field>
            <field name="inherit_id" ref="stock.view_picking_form"/>
            <field name="arch" type="xml">

                <xpath expr="//header" position="inside">
                    <field name="hide_button" invisible="1"/>
                    <field name="env_user" invisible="1"/>
                </xpath>

                <!-- ++++++++++++++++++++ -->
                <!-- For Production User  -->
                <!-- ++++++++++++++++++++ -->

                <!-- Hide Action Confirm from prod user -->
                <xpath expr="//header/button[@name='action_confirm']" position="attributes">
                    <attribute name="invisible">
                        not show_mark_as_todo or (env_user == 11 and picking_type_id != 35)
                    </attribute>
                </xpath>

                <!-- Hide Action Validate (first button) from prod user -->
                <xpath expr="//header/button[@name='button_validate'][1]" position="attributes">
                    <attribute name="invisible">
                        (env_user == 11 and picking_type_id != 35) or
                        state in ('waiting', 'confirmed') or
                        not show_validate
                    </attribute>
                </xpath>

                <!-- Hide Action Validate (second button) from prod user -->
                <xpath expr="//header/button[@name='button_validate'][2]" position="attributes">
                    <attribute name="invisible">
                        (env_user == 11 and picking_type_id != 35) or
                        state not in ('waiting', 'confirmed') or
                        not show_validate
                    </attribute>
                </xpath>

                <!-- ++++++++++++++++++++ -->
                <!-- For Store User  -->
                <!-- ++++++++++++++++++++ -->

                <!-- Detailed Operations (move_line_ids_without_package) -->
                <!-- <xpath expr="//field[@name='move_line_ids']" position="attributes">
                    <attribute name="readonly">
                        (env_user == 10 and picking_type_id == 42) or
                        (not show_operations or state == 'cancel' or (state == 'done' and is_locked))
                    </attribute>
                    <attribute name="invisible">not show_reserved</attribute>
                </xpath> -->

                <!-- Operations (move_ids_without_package) -->
                <xpath expr="//field[@name='move_ids']" position="attributes">
                    <attribute name="readonly">
                        (state == 'done' and is_locked) or
                        (env_user == 10 and picking_type_id == 42)
                    </attribute>
                </xpath>

            </field>
        </record>
    </data>
</odoo>